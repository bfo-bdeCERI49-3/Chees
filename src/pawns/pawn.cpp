#include "pawns/pawn.hpp"

int Pawn::move(Board m_board){
    if ( color == Color::White ) {
        if ( first_move ) {
            
        } else {
            board.cases[]
        }
    } else {
        if ( first_move ) {
            
        } else {

        }
    }

    return 0;
}